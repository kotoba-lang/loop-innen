{
  "name": "loop-innen",
  "private": true,
  "description": "nbb scripts for the innen dependency corpus query layer (kotoba-lang/datalog). No runtime JS of its own.",
  "scripts": {
    "test": "nbb test/run_tests.cljs",
    "query:demo": "nbb bin/query.cljs demo"
  }
}
